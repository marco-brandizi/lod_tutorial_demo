PREFIX dbponto: <http://dbpedia.org/ontology/>
PREFIX foaf: <http://xmlns.com/foaf/0.1/>

SELECT DISTINCT ?imgUrl 
WHERE {
   ?dbpProtein 
     dbponto:uniprot ?uniProtId; 
     foaf:depiction ?imgUrl.
}
LIMIT 1
